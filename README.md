# TMUX dotfiles

# Setup

1) Clone the repo:
```bash
git clone --recurse-submodules git@github.com:yomzyo/tmux-dotfiles.git ~/.config/tmux 
```

2) Run tmux 

3) Press `prefix` + `I` to fetch plugins
